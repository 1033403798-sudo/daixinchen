// Legacy lower-page background intentionally disabled.
// The hero still uses its own Ferrofluid component; all later sections use FloatingLines.
export default function FerrofluidField() {
  return null;
}
